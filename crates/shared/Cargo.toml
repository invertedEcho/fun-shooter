[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
avian3d = {version = "0.4.1", features = ["serialize"]}
bevy = "0.17.3"
lightyear = {version = "0.25.5", features = ["trace", "client", "server"]}
serde = "1.0.228"
rand = {workspace=true}
hex = "0.4.3"
